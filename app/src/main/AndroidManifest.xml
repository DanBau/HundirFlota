<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daniel_bautista.hundirflota">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitys.PerdedorActivity"></activity>
        <activity android:name=".Activitys.AyudaActivity" />
        <activity android:name=".Activitys.GanadorActivity" />
        <activity android:name=".Activitys.RankingActivity" />
        <activity android:name=".Activitys.AcercaActivity" />
        <activity android:name=".Activitys.JugarActivity" />
        <activity android:name=".Activitys.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>